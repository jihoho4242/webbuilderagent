# Agent OS Runtime Contract

`engine-run` is the canonical durable Agent OS runtime. `aiweb agent` is a goal facade that must point to engine-run-compatible artifacts instead of creating a competing runtime truth.

Fixed pipelines and deterministic browser probes are tools/evidence fixtures. They are not autonomous planning engines.
